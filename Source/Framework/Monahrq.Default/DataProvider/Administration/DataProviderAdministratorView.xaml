<UserControl x:Class="Monahrq.Default.DataProvider.Administration.DataProviderAdministratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel LastChildFill="true">
        <TextBlock DockPanel.Dock="Top">Select a datasource</TextBlock>
        <StackPanel Margin="5,0,5,0" Orientation="Vertical" DockPanel.Dock="Right" Width="50">
            <Button Click="{Binding SelectCommand}">Select</Button>
            <Button Click="{Binding NewCommand}">New</Button>
            <Button Click="{Binding DeleteCommand}">Delete</Button>
            <Button Click="{Binding ModifyCommand}">Modify</Button>
        </StackPanel>
        <StackPanel Margin="0,0,0,264">
            <ComboBox>Select a datasource</ComboBox>
        </StackPanel>
    </DockPanel>
</UserControl>
