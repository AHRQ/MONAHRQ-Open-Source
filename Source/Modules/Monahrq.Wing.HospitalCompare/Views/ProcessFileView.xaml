<UserControl x:Class="Monahrq.Wing.HospitalCompare.Views.ProcessFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lb="clr-namespace:Monahrq.Theme.Behaviors;assembly=Monahrq.Theme"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Data import log:" />
        <ListBox Grid.Row="1" ItemsSource="{Binding LogFile}" Background="White" lb:ListBoxBehavior.ScrollOnNewItem="true" />
    </Grid>
</UserControl>
